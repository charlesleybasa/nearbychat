
  # User Authentication with Chat

  This is a code bundle for User Authentication with Chat. The original project is available at https://www.figma.com/design/N0fgDtsYuLe5Uw69J15bwp/User-Authentication-with-Chat.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  